# Usage: /my:proof <file_path>
#
# Description:
# Corrects typos, inconsistencies, and Markdown formatting in the specified document.

description = "Corrects typos, inconsistencies, and Markdown formatting in the document."

prompt = """
Correct the text in {{args}} based on the following criteria:

- Fix typos and omissions.
- Unify terminology inconsistencies.
- Organize Markdown formatting:
  - Comply with GitHub Flavored Markdown (GFM).
  - When using lists, do not create new numbered lists. Maintain existing numbered lists.
  - Do not use Bold (**).
"""
