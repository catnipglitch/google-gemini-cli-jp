# Usage: /my:commit
#
# Description:
# Generates a Git commit message based on staged changes.

description = "Generates a Git commit message based on staged changes."

# The prompt uses !{...} to execute the command and inject its output.
prompt = """
Generate a Conventional Commit message in Japanese based on the following git diff.
Summarize the essence and intent of the changes rather than just transcribing the content.

```diff
!{git diff --staged}
```
"""