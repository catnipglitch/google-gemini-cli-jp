# Cleanup Back to Main

**説明**  
作業が終わったブランチから `main` へ戻り、最新状態を取得し、当該ブランチを削除する手順を指示するコマンドです。

## 手順
1. 現在のブランチ名を取得して記録する。
2. そのブランチが `main` であれば作業を中止し、その旨を報告する。
3. `main` ブランチにチェックアウトする。
4. `main` 上で最新コミットを取得（`git pull` など）する。
5. 初めに記録したブランチを削除して作業ブランチを掃除する。
